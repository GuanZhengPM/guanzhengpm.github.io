// 部署 analytics-worker 后，将 endpoint 改为 Worker 地址，例如：
// endpoint: "https://guanzheng-blog-analytics.<你的子域>.workers.dev"
export const analyticsConfig = {
  endpoint: "",
  siteId: "guanzheng-blog",
};
